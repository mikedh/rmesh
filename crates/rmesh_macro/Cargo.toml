[package]
name = "rmesh_macro"
description = "The proc macros for rmesh primarily to manage caching."
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
quote = "1.0.40"
syn = { version = "2.0.100",  features = ["full"] }
